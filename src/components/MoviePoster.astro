---
interface Props {
  src: string;
  alt: string;
  width: number;
  height: number;
}

const { src, alt, width, height } = Astro.props;
---

<div class="movie-poster-container">
  <img class="movie-poster" src={src} alt={alt} width={width} height={height} />
</div>
